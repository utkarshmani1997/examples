name: "blog"
about: "an example blog app"

design-paths:
  - "design"

dsl-config:
  paths:
    - "$HOME/.geb/dsl"
  default:

    - dsl: api
      gen:
        - golang/echo
      output-dir: "."

log-config:
  default:
    level: warn
    stack: true
  #engine:
    #design:
      #level: info
